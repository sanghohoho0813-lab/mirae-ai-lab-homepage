# 회원가입 / 인증 정책 (AUTH POLICY)

> 미래 AI 랩(Mirae AI Lab) 회원 가입·인증 및 체험 악용 방지 정책.
> 현재(1단계)는 localStorage mock으로 일부만 적용되어 있고, 실제 강제는 Supabase Auth에서 한다.

## 핵심 문제

아무 이메일이나 입력해 계속 새 계정을 만들 수 있으면 **7일 체험 제한이 무력화**된다.
따라서 "고유한 신원"을 보장하는 가입 절차가 필요하다.

---

## 회원가입 기본 정책

- **이메일 인증 필수** (Supabase Auth confirm email)
- **이메일 중복 가입 불가**
- **휴대폰 번호 중복 가입 제한**
- **비밀번호 최소 조건** (최소 8자 — 추후 영문/숫자 조합 등 강화 가능)
- 가입 시 **이름, 이메일, 휴대폰, 직업/소속 필수**

### 현재(mock) 적용 상태

| 항목 | 현재(mock) | 실제(Supabase) |
| --- | --- | --- |
| 이메일 인증 | ✕ (검증 없음) | ✅ confirm email |
| 이메일 중복 차단 | ✅ localStorage 검사 | ✅ unique 제약 |
| 휴대폰 중복 차단 | ✅ localStorage 검사 | ✅ unique 제약 + (선택)본인인증 |
| 비밀번호 최소 8자 | ✅ 폼 + 코드 검사 | ✅ Auth 정책 |
| 로그인 | 가입된 이메일만(관리자 이메일은 데모 자동생성) | ✅ 비밀번호/OAuth |

> 구현 위치: `src/lib/auth.tsx`(signup/login), `src/lib/platform.ts`(`getUserByEmail`/`getUserByPhone`).

---

## 체험 악용 방지

- **같은 이메일 재가입 불가** (mock 적용)
- **같은 휴대폰 번호 재가입 불가** (mock 적용)
- 동일 브라우저/localStorage 기준 반복 체험 시작 제한은 **보조 장치로만** 사용 (지우면 우회되므로 신뢰 ✕)
- 같은 IP 반복 가입 제한은 **추후 검토** (서버에서)
- **관리자 승인 기반 베타 운영** 가능 (민감 도구는 "승인 후 이용")
- 체험은 **모듈별 1회 시작 → 재시작 불가** (`startTrial`은 이미 시작된 경우 무시)
- 무료 연장은 **리뷰 1회 + 설문 1회 = 최대 +14일**, 총 **최대 21일** (서버에서 동일 검증 필요)

> ⚠️ 진짜 악용 방지는 **서버에서 신원(이메일 인증/유니크 휴대폰)과 체험 기록을 검증**해야 가능하다.
> 클라이언트 localStorage 검사는 데모 수준이다.

---

## 추천 가입 방식 (우선순위)

1. **Supabase Auth 이메일 인증** (1순위)
2. **Google 로그인** (2순위)
3. **Kakao 로그인** (3순위)
4. **휴대폰 인증**은 추후 도입 (체험 악용 방지에 가장 강력 → 민감 도구에 우선 적용 검토)

> 현재 UI: 로그인/회원가입 화면에 Google·Kakao 버튼을 "준비 중"으로 노출(`src/pages/LoginPage.tsx`).

---

## 베타 기간 운영 정책

- 초기에는 **회원가입 후 바로 전체 오픈보다**, **관리자 승인** 또는 **도구별 7일 체험 시작** 방식을 추천.
- **민감한 도구**는 `"승인 후 이용"` 상태로 운영 (예: 주식 EXIT = `private`, 외부 공개 제한 유지).
- 관리자는 사용자별/도구별로 권한을 직접 부여·연장·회수·무제한 처리할 수 있다.

---

## Supabase 전환 시 체크리스트

- [ ] `profiles`에 email **unique**, phone **unique(부분/정규화)** 제약
- [ ] Auth confirm email 활성화
- [ ] 비밀번호 정책(최소 길이/복잡도) 설정
- [ ] Google/Kakao OAuth 앱 등록 및 리다이렉트 설정
- [ ] 체험 시작/연장/관리자 액션을 **Edge Function(서버)** 에서 검증·기록 (RLS write 제한)
- [ ] (선택) 휴대폰 본인인증(SMS) 도입 — 민감 도구 우선
- [ ] 가입/연장 이벤트 로깅(이상 패턴 탐지)
